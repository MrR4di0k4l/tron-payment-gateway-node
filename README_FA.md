
## مستندات فارسی

### 🌟 ویژگی‌ها

* **پشتیبانی از TRX:** طراحی شده اختصاصی برای دریافت و پردازش تراکنش‌های ارز ترون (TRX).
* **سیستم چرخش کیف پول (Wallet Pool):** مدیریت بهینه آدرس‌ها (پیش‌فرض: ۲۰ عدد) برای جلوگیری از ساخت بی‌رویه کیف پول و استفاده مجدد از آن‌ها.
* **بازیافت خودکار:** کیف پول‌هایی که واریزی نداشته‌اند پس از زمان مشخص (پیش‌فرض: ۷ دقیقه) آزاد شده و به مشتری بعدی اختصاص می‌یابند.
* **تجمیع خودکار (Auto-Sweep):** تشخیص آنی واریز و انتقال خودکار موجودی به کیف پول اصلی (Cold Wallet) شما.
* **محاسبه هوشمند کارمزد:** محاسبه دقیق و لحظه‌ای کارمزد شبکه ترون برای جلوگیری از شکست تراکنش‌ها و کاهش هزینه‌ها.
* **وب‌هوک (Webhook):** اطلاع‌رسانی به سایت شما از طریق متد GET هنگام تشخیص واریز و انجام موفق انتقال.
* **داکرایز شده:** نصب و راه‌اندازی بسیار آسان و سریع با داکر.
* **دیتابیس پایدار:** استفاده از SQLite برای ذخیره کلیدها و وضعیت تراکنش‌ها به صورت لوکال.

### 🚀 نصب و راه‌اندازی

#### پیش‌نیازها

* نصب بودن Docker و Docker Compose.
* داشتن کلید API از TronGrid (برای جلوگیری از محدودیت درخواست‌ها توصیه می‌شود).

#### ۱. تنظیمات

فایل `app.js` را باز کنید و تنظیمات زیر را بر اساس نیاز خود ویرایش کنید:

```javascript
const MAX_WALLETS = 20;            // حداکثر تعداد کیف پول‌های مجاز در استخر
const ORDER_TIMEOUT_MINUTES = 7;   // زمان انقضای رزرو کیف پول برای هر مشتری (دقیقه)
const TRON_API_KEY = 'کلید_شما';    // دریافت رایگان از trongrid.io

```

#### ۲. اجرا با داکر

در پوشه اصلی پروژه دستور زیر را اجرا کنید:

```bash
docker-compose up -d --build

```

سرویس روی پورت **3000** سرور شما در دسترس خواهد بود.

### 🛠️ مستندات API

#### ۱. دریافت آدرس پرداخت

یک کیف پول از استخر انتخاب کرده و به مشتری اختصاص می‌دهد. اگر کیف پول آزادی باشد، همان را بازیافت می‌کند.

* **آدرس:** `GET /newAddress`
* **پارامترها:**
* `notifyurl`: آدرس سایت شما برای دریافت نتیجه پرداخت (Callback).
* `collect_address`: آدرس کیف پول اصلی شما (پول‌ها نهایتاً به اینجا واریز می‌شوند).
* `extra`: (اختیاری) شناسه سفارش، نام کاربر یا هر دیتای دلخواه.


* **مثال:**
```
http://localhost:3000/newAddress?notifyurl=[https://mysite.com/callback&collect_address=TZ...&extra=order123](https://mysite.com/callback&collect_address=TZ...&extra=order123)

```



#### ۲. مشاهده موجودی

* **آدرس:** `GET /balance`
* **پارامتر:** `address` (آدرس کیف پول مورد نظر)

#### ۳. تجمیع دستی (Manual Sweep)

اگر به هر دلیلی (مثل قطعی اینترنت) تجمیع خودکار انجام نشد، می‌توانید با این دستور سیستم را مجبور به بررسی و انتقال موجودی کنید.

* **آدرس:** `GET /trans`
* **پارامتر:** `address`

### QC ساختار وب‌هوک

سیستم یک درخواست `GET` به آدرس `notifyurl` شما می‌فرستد که شامل پارامترهای زیر در URL است:

* `code`: `0` (عملیات موفق).
* `type`:
* `"DEPOSIT"`: واریز مشتری تشخیص داده شد.
* `"COLLECTION"`: پول با موفقیت به کیف اصلی شما منتقل شد.


* `amount`: مقدار ترون.
* `txid`: هش تراکنش در شبکه (Transaction ID).
* `extra`: همان دیتایی که موقع ساخت آدرس فرستادید (مثلاً شماره سفارش).

---

### ⚠️ Security Warning / هشدار امنیتی

**Important:** This project stores private keys in a local SQLite database (`data/address.db`).

* Ensure your server is secure.
* Do not expose the `data` folder publicly.
* Use `docker-compose` volumes to persist data safely.

**مهم:** این پروژه کلیدهای خصوصی کیف پول‌ها را در دیتابیس لوکال (`address.db`) ذخیره می‌کند.

* از امنیت سرور خود اطمینان حاصل کنید.
* دسترسی عمومی به فایل دیتابیس را مسدود کنید.
* حتماً از Volume داکر استفاده کنید تا با ریستارت شدن کانتینر، اطلاعات و کلیدها از بین نروند.
